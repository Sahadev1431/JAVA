import java.util.function.Predicate;

public class predicateInterface {
    public static void main(String[] args) {
        // Predicate <Integer> p1 =(s)->(s>10);
        // // Predicate <Integer> p1 =s->(s>10); we can simply right it like this also because single parameter we can ignore () brackets
        // System.out.println(p1.test(45));


        


    }
}